# API base URL (used when VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:4002/api/v1

# Use the built-in in-memory mock API so the UI runs with zero backend setup.
# Set to "false" to talk to the real Express/PostgreSQL backend.
VITE_USE_MOCK=true
